# NectarPin 配置文件示例
# 复制此文件为 config.yaml 并修改相应配置

server:
  host: "0.0.0.0"
  port: "8080"
  mode: "debug"  # debug, release, test

database:
  host: "10.1.0.4"
  port: "5432"
  user: "nectarpin"
  password: "5cg2c60gJfdPPX"
  dbname: "nectarpin"
  sslmode: "disable"  # PostgreSQL SSL模式: disable, require, verify-ca, verify-full

# 日志配置
log:
  level: "info"  # debug, info, warn, error
  file: "logs/app.log"

